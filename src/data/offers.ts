import { offersMocks } from '../mocks/offers';
import { reviewsData } from '../mocks/reviews';

export const offersData = {
  offers: offersMocks,
  nears: {
    cards: offersMocks
  },
  reviews: reviewsData,
};
